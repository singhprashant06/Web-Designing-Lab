1) C Language 
  
## Basics of coding  
**Code:-** instruction written by the programmer  
**Programme:-** a complete set of instruction/code that perform a task  
*Code is input, and the result produced by the program/code is output*  
**Compiler:- ** who converts the code/program to output example of compiler GCC, clang, MSVC (Microsoft C/C++ compiler)  
**Code editor:- **where the programmer, write the code  

2) Structure of C language
# include <stdio.h>
int main() {
printf(“Prashant”);
return = 0;
}

# include <stdio.h>  
std-> standard 
io-> input/output
.h-> header file

# Include it tells C language to include a header file in our program
And the header file we want to include is mentioned next


In a simple language stdio.h is a standard header file that provides access to standard input output functions such as printf() and scanf() in C language.



int main()
main() this is starting point of our C programme
int is a type of variable means integer. It tells that our main function will return integer value when it will finish. That’s why we later write return = 0.

{ } curly braces
{ start of the main code
} end of the main code

*  \n is used to jump to new line \ next line its inserted between “” inserted between fnx
